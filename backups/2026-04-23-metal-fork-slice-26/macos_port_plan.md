# Permafrost macOS Port And Execution Plan

## Summary

- Status: `execution in progress`
- Current milestone: `Phase 5 - Metal renderer transition`
- Current milestone status: `in progress; the backend split is landed, the OpenGL fallback is preserved, and Metal now reaches startup UI, visible terrain, a first real terrain-texturing pass, a first real textured static/skinned-mesh pass, first world-selection overlays, drag-box UI selection, first healthbars, a first minimap render with terrain, unit dots, and camera-frustum overlay, a first translucent map-overlay-quads slice, a first scripted gameplay smoke pass, and a first Session UI save/load roundtrip on Apple Silicon`
- Current scope for this workstream:
  1. keep the existing Permafrost RTS game solid on native Apple Silicon `arm64`
  2. align repo-tracked defaults/config with the verified Mac runtime
  3. add repeatable native validation for launch and Session roundtrips
  4. begin the backend-neutral renderer split and Metal bootstrap without regressing the stable OpenGL fallback

## Phase Status

- `Phase 0: Discovery and feasibility` - `completed`
- `Phase 1: Rosetta x86_64 demo bring-up` - `blocked`
- `Phase 2: Native arm64 engine bring-up` - `completed enough to support normal launch, save/load, shadows, full water, and batch rendering on Apple Silicon`
- `Phase 3: Apple Silicon game polish` - `completed for the existing game`
- `Phase 4: Python 3 cleanup beyond the existing game path` - `deferred unless it blocks the game`
- `Phase 5: Metal renderer transition` - `in progress`

## Current Apple Silicon Polish Scope

- `Now`
  - preserve the completed Apple Silicon game baseline
  - only return to low-level OpenGL fixes if a real gameplay bug shows up
  - advance the Metal backend through backend abstraction, cold launch, then playable runtime scene
- `Deferred but still tracked`
  - Rosetta `x86_64` lane remains blocked on `/usr/local` admin/toolchain issues
  - macOS editor parity remains out of scope for the current game-polish phase
  - `pf.Task` parity on Python 3 remains explicitly unsupported
  - the old `S_PickleObjgraph`-based pickler remains deferred behind the working native session replacement
  - Metal runtime playable-scene parity and editor parity are still pending beyond the current bootstrap slice

## Current Implementation Notes

- Added `PLAT=MACOS_X86_64` Makefile support using host-installed x86_64 dependencies.
- Added `PLAT=MACOS_ARM64` dependency scaffolding and explicit build guards for the still-pending native phase.
- Added macOS dependency validation helpers for Rosetta and native arm64.
- Added a safe `git describe` fallback for archive-style source trees with no `.git` metadata.
- Kept the existing x86_64 scheduler, `C99`, Python `2.7`, and current runtime behavior for the first bring-up.
- Added an engine-owned macOS OpenGL loader in `src/render/gl_loader.[ch]` so the macOS path no longer depends on GLEW.
- Rewired renderer feature checks and fallbacks to use the new loader capabilities for debug output, compute support, timer queries, copy-image, buffer-storage, and multi-draw-indirect.
- Added macOS fallbacks for terrain buffer allocation and texture-array debug/copy paths when newer OpenGL helpers are unavailable.
- Added a first native Darwin arm64 scheduler/context-switch backend while keeping the existing stackful task model.
- Moved the native build to `gnu11`-style strictness cleanup across engine/gameplay code that now compiles under Apple clang.
- Started the Python 3.13 bridge migration far enough for the native arm64 build to move past the script layer and into core engine/render modules.
- The native arm64 build now links and produces `bin/pf-arm64`.
- The Python 3 path currently treats `pf.Task` as unsupported during bring-up instead of attempting a broken partial port of old CPython frame/thread-state internals.
- The only concrete Python-side helper still depending on `pf.Task` for the live codebase was `scripts/common/disappearing_text_task.py`; on Python 3 it now uses a module-level `EVENT_UPDATE_START` handler instead of task fibers.
- The deeper `src/script/py_task.c` fiber/task port remains deferred because the active RTS bring-up does not currently require engine-level `pf.Task` support.
- The Session window and RTS demo Session entry point are now re-enabled on Python 3 for the native path because the normal demo/UI save-load roundtrip is verified on Apple Silicon.
- The first native replacement step is now in place: `scripts/editor/scene.py` exposes a reusable Python-3-safe `save_scene_from_objects()` helper that can snapshot live RTS entities into a PFSCENE file without using `S_PickleObjgraph`.
- The native Python 3 session snapshot path now also preserves optional scene cameras and can re-wrap already-restored engine regions without colliding with `G_LoadGlobalState()`.
- That scene-snapshot foundation is verified end to end on Apple Silicon:
  - a native arm64 export run wrote `tmp_native_scene_snapshot_test.pfscene` from the live RTS scene with `EXPORTED_SCENE_OBJS=388`
  - a fresh native arm64 import run then loaded that snapshot back with `IMPORTED_SCENE_OBJS=388`
- The native Python 3 session replacement now also works for the current RTS demo roundtrip: the live scene is saved through the new PFSCENE-backed path, reloaded on arm64, and re-bootstrapped into the RTS runtime without using `S_PickleObjgraph`.
- The post-load continuation bug on Apple Silicon is now fixed:
  - task-context render flushing now swaps and drains the correct render workspace instead of deferring the entire session-load queue to the first resumed frame
  - task-context render flushing now restarts the render thread across `RSTAT_YIELD` boundaries during large restore batches instead of stalling mid-flush
  - movement render-state teardown no longer calls compute-shader cleanup unsafely on non-compute macOS paths, and the stale SSBO handle resets are corrected
- The remaining session work is now narrower:
  - broader manual/user-driven validation beyond the scripted UI roundtrip
  - the old custom Python pickler remains deferred
- The current Apple Silicon game-polish pass now also locks the proven native defaults into the repo:
  - `pf.conf` now matches the verified arm64 runtime for `pf.game.combat_hz`, `pf.video.use_batch_rendering`, and `pf.video.vsync`
  - the hard-coded local `pf.video.aspect_ratio` / `pf.video.resolution` overrides were removed from `pf.conf` so the code again uses the native desktop size at startup
  - `pf.video.vsync` now defaults to `true` in `src/render/render.c`
- `make run PLAT=MACOS_ARM64` now launches the working native Apple Silicon game path instead of returning a stub message.
- Repo-backed Apple Silicon validation helpers now live under `scripts/macos`:
  - `pf_native_launch_probe.py` verifies a cold native launch and emits the live Mac runtime settings marker
  - `pf_native_session_ui_region_camera_roundtrip.py` verifies the normal Session UI roundtrip with non-empty region/camera metadata
  - `pf_native_settings_apply_probe.py` and `pf_native_settings_verify_restore_probe.py` verify settings application through the existing controllers, relaunch persistence, and restoration back to the baseline values
- A new Metal workstream baseline snapshot is saved under `backups/2026-04-22-metal-fork-start/`.
- The renderer control surface now has an explicit backend selector for Apple Silicon work:
  - Makefile supports `RENDER_BACKEND=OPENGL` and `RENDER_BACKEND=METAL`
  - backend builds now use backend-specific object directories under `obj/`, so `OPENGL` and `METAL` no longer overwrite each other's compiled artifacts
  - `make run` and `make run_hfmp` now rebuild the selected backend before launching
  - the default repo behavior remains `OPENGL`
- The render bootstrap is now split at the backend seam:
  - shared render settings, command/workspace management, and public renderer control wrappers remain in `src/render/render.c`
  - OpenGL bootstrap/context/present logic moved into `src/render/backend_gl.c`
  - the new Metal bootstrap lives in `src/render/backend_metal.m`
- Shared macOS window glue no longer assumes OpenGL:
  - window flags now come from the active render backend
  - drawable-size queries and present calls route through backend wrappers instead of `SDL_GL_*` in shared runtime code
  - early loading-screen drawing now skips both OpenGL and Metal windows on macOS
- Render info now exposes the active backend string through the `pf` scripting surface, and the runtime launch probe now records `pf.render.backend` and `pf.render.renderer`.
- The current Metal slice is intentionally narrow but now visibly useful:
  - Metal creates an SDL Metal view, a Metal device, command queue, and CAMetalLayer-backed frame/present loop
  - Nuklear font-atlas upload and UI rendering now execute on Metal, and the runtime reaches a visible startup menu/UI instead of only a cold-launch marker
  - terrain `R_GL_Draw` calls now shade visible world geometry with a simple material-index palette on Metal
  - non-animated mesh draws now shade CPU-staged vertex streams from their CPU-side material diffuse colors on Metal
  - animated meshes now preserve CPU copies of `anim_vert` data, mark pose-buffer use during asset load, and software-skin the first visible units per entity on Metal
  - unit and building selection shapes now route through the Metal backend too, using the existing CPU-side selection inputs plus terrain-conforming height samples
  - drag-box UI selection now also routes through the Metal backend as a screen-space overlay and is visibly verified from a real drag capture
  - the minimap now has a first Metal gameplay-feedback slice: the backend bakes a simple top-down terrain texture, renders the rotated minimap diamond on-screen, overlays unit dots through the same normalized minimap-space math as the OpenGL path, and now also clips/draws the camera-frustum overlay inside the minimap diamond instead of leaking a giant off-minimap quad
  - healthbars now also route through the Metal backend as a gameplay-visible screen-space overlay using the same world-to-screen placement math and size rules as the OpenGL path, with a simple bordered fill-bar draw for the first Metal pass
  - `R_GL_DrawMapOverlayQuads` now also routes through Metal as a batched world-color overlay path, so the first fog/debug tile-overlay slice can render translucent filled quads plus outlines against the real Metal scene
  - the current Metal smoothness work has now started to remove avoidable per-frame GPU setup overhead too: terrain and non-animated mesh draws now cache persistent Metal vertex buffers instead of allocating fresh `MTLBuffer` objects from CPU-side vertex copies every frame
  - the next slice now also restores first-pass real terrain texturing on Metal: `R_GL_MapInit` now loads the map's source terrain textures into a Metal texture array, and the Metal terrain shader samples those textures through the existing terrain UV/material-index path instead of only shading from the temporary flat material palette
  - the next slice now also restores first-pass real textured static/skinned meshes on Metal: mesh asset `base_path` now stays available in `render_private`, the Metal backend lazily builds a per-mesh material texture array from the same `texname` list the OpenGL path already uses, and both static and software-skinned animated draws now sample those textures instead of only using diffuse-color fallback shading
  - `scripts/macos/pf_metal_gameplay_smoke_probe.py` now verifies the first real Metal gameplay flow directly in the running RTS demo: camera motion, unit selection, a move order, pause/resume, and an attack/contact step against a staged enemy target all complete under `RENDER_BACKEND=METAL`
  - `scripts/macos/pf_native_session_ui_region_camera_roundtrip.py` now also verifies the existing Python 3 Session UI save/load path on `RENDER_BACKEND=METAL`, restoring non-empty scene-region and scene-camera metadata through the normal demo event flow
  - the current Metal runtime is still intentionally limited to first-pass world feedback plus that first scripted gameplay flow and Session roundtrip; broader fog/gameplay parity, water integration, and batch parity are still pending
  - research against current Apple Metal guidance points to this near-term order for quality/smoothness work on this renderer:
    1. keep cutting per-frame CPU/GPU overhead in the current backend
    2. finish runtime gameplay parity before heavier visual features
    3. add anti-aliasing and better frame pacing before bigger “realism” pushes
    4. restore richer scene features like skybox, shadows, and full water
    5. leave HDR, MetalFX upscaling, indirect command buffers, and faster asset streaming until after the runtime scene is stable enough to benefit from them
  - the current Metal build still links the existing OpenGL renderer symbols so the rest of the engine can compile while backend-neutral draw migration is incomplete
- `scripts/macos/pf_metal_launch_probe.py` now verifies the Metal backend can survive engine startup and emit the live backend marker on Apple Silicon.
- A local backup snapshot of the files touched in this polish slice is saved under `backups/2026-04-22-apple-silicon-polish/`.
- The Apple Silicon settings path now has one real fix in place:
  - `pf.game.healthbar_mode` is now applied as explicit `HB_MODE_*` values instead of bools, so the Game settings page persists the intended health-bar mode instead of relying on Python bool/int coercion
- Runtime validation of `bin/pf-arm64` on this Apple Silicon Mac is active and has now reached a stable early-launch smoke pass.
- Native Python 3 unit initialization on Apple Silicon now uses explicit RTS-side initialization instead of relying on the old cooperative `super()` chain across mixed Python/C extension bases.
- Common RTS/settings window constructors were updated for Python 3 integer division so `pf.Window` bounds no longer fail on float coordinates.
- Native Apple Silicon water rendering now runs through the full water path in `src/render/gl_water.c` again.
- The restored Apple Silicon water path now uses the regular reflection/refraction pipeline instead of the earlier simplified fallback:
  - the latest verified frame shows rippled/refractive water detail on arm64 instead of the earlier flat translucent surface
  - the fresh launch path remains quiet on stderr apart from the expected compute-shader skip
- A longer native gameplay smoke pass has now verified live camera motion, visible unit selection, a successful move order, fog-of-war reveal from unit movement, and a quiet 65-second stability hold on this Mac.
- Native Apple Silicon now also has two verified render-path fixes in place:
  - generic mesh draws only bind the animation pose buffer for animated assets instead of all meshes
  - terrain/generic mesh shaders now bind and use the real shadow-map path on macOS arm64 instead of the earlier degraded no-shadow path
- Native Apple Silicon now also has the batch-rendering path back on:
  - batched mesh shaders are no longer skipped during shader init on arm64
  - the default and current local config now both enable `pf.video.use_batch_rendering`
- Native Apple Silicon now also has three responsiveness/compatibility fixes in place:
  - the render-thread yield path now signals the main thread before `SDL_GL_SwapWindow` on Apple Silicon so loading-time swap waits do not deadlock the app
  - 2D/overlay line-width requests are clamped through a macOS-compatible path instead of tripping core-profile `GL_INVALID_VALUE`
  - map-texture Wang tileset generation uses a fast Apple Silicon fallback upload path instead of stalling startup in the expensive quilt generator
- Native Apple Silicon now also has two verified runtime-stability fixes in place:
  - `Sched_Tick()` now waits on `s_ready_cond` while actually holding `s_ready_lock`, fixing the sampled Apple Silicon hang where the main thread could wait forever after workers had already gone idle
  - minimap click/drag world conversion now uses the terrain diamond instead of the decorative outer minimap bounds, and the resulting world coordinate is clamped back into the playable map before camera movement
- `Settings_Create()` now correctly reuses existing settings slots when `pf.conf` is present, which removed the duplicate-registration assert seen on relaunch.
- The first visible native demo screenshot is saved at `macos_port_screenshot_2026-04-20.png`.
- Installed `cmake` and `pyenv` in `/opt/homebrew`.
- Installed native arm64 `openal-soft` and `mimalloc` in `/opt/homebrew`.

## Verified Results

- `completed` `make deps PLAT=MACOS_X86_64` now performs a real dependency validation pass for the Rosetta lane.
- `completed` `make deps PLAT=MACOS_ARM64` now performs a real dependency validation pass for the native arm64 lane.
- `completed` Helper scripts pass shell syntax validation.
- `completed` `make -n pf PLAT=MACOS_X86_64` expands to Rosetta `clang` compile commands with the new macOS target wiring.
- `completed` `make pf PLAT=MACOS_ARM64` now fails immediately with the explicit pending-phase message instead of falling through into a broken build.
- `completed` `make run_editor PLAT=MACOS_X86_64` now fails with the explicit unsupported-phase message.
- `completed` macOS dependency checks no longer require `glew`; `make deps PLAT=MACOS_X86_64` now reports only the missing x86_64 Homebrew libraries and Python 2.7 toolchain.
- `completed` `make -B obj/render/gl_loader.o obj/render/render.o obj/render/gl_batch.o PLAT=MACOS_ARM64 MACOS_ARM64_BUILD_READY=1 EXTRA_FLAGS='-Wno-deprecated-non-prototype -Wno-unused-but-set-variable'` succeeds.
- `completed` `make -B obj/render/gl_texture.o obj/render/gl_ringbuffer.o obj/render/gl_terrain.o PLAT=MACOS_ARM64 MACOS_ARM64_BUILD_READY=1 EXTRA_FLAGS='-Wno-deprecated-non-prototype -Wno-unused-but-set-variable'` succeeds.
- `completed` Native arm64 warning cleanup now covers typed vector comparators, SDL global-event registration signatures, and SDL window flag accumulation.
- `completed` `make -B obj/event.o obj/cursor.o obj/cam_control.o obj/main.o obj/audio/al_audio.o obj/map/raycast.o obj/render/render.o obj/render/gl_batch.o obj/game/clearpath.o obj/game/game.o obj/game/region.o PLAT=MACOS_ARM64 MACOS_ARM64_BUILD_READY=1` now compiles through the prior `gnu11` warning blockers until it reaches the Python bridge.
- `completed` `make -B obj/script/py_console.o PLAT=MACOS_ARM64 MACOS_ARM64_BUILD_READY=1` now succeeds because the interactive console is stubbed on Python 3.
- `completed` `make -B obj/script/py_pickle.o obj/script/py_script.o PLAT=MACOS_ARM64 MACOS_ARM64_BUILD_READY=1` succeeds with the Python 3 compatibility layer, module registration changes, modern `sys.argv` handling, weakref updates, and save/load disabled on Python 3.
- `completed` `make -B obj/sched.o obj/context_switch.o PLAT=MACOS_ARM64 MACOS_ARM64_BUILD_READY=1` succeeds with the new Darwin arm64 scheduler backend and AArch64 context-switch trampolines.
- `completed` `make -B obj/render/gl_image_quilt.o PLAT=MACOS_ARM64 MACOS_ARM64_BUILD_READY=1` succeeds after fixing macOS header portability in the render path.
- `completed` `make -B pf PLAT=MACOS_ARM64 MACOS_ARM64_BUILD_READY=1` now links successfully and produces `bin/pf-arm64`.
- `completed` The previous native blocker in `src/script/py_entity.c` is cleared; the current native frontier is runtime startup validation on Apple Silicon.
- `completed` `pf.Task` and Python-backed save/load are explicitly gated off on Python 3 for the current native macOS bring-up so the port fails clearly instead of depending on removed CPython internals.
- `completed` Native runtime launch blockers cleared so far include:
  - macOS GL loader fallback to framework symbols when `SDL_GL_GetProcAddress` omits core functions
  - skipping the early software loading-screen path on macOS OpenGL windows so SDL still creates a real GL window/context
  - removing unsupported GLSL `#if USE_GEOMETRY` dependency from the affected vertex shaders
  - fixing Python-2-era bare `METH_KEYWORDS` method flags that CPython 3 rejects during type registration
- `completed` `./bin/pf-arm64 ./ ./scripts/rts/main.py` now launches on this Apple Silicon Mac, reports only the expected missing `pf.conf` warning and compute-shader skip, and stays alive for an approximately 60-second smoke run without a new crash on stderr.
- `completed` Native arm64 scene loading now succeeds on this Mac after fixing Python 3 package imports, unit initializer chaining, and Python 2 integer-division assumptions in the RTS/common window scripts.
- `completed` Native arm64 now reaches a visible in-game frame with the RTS demo UI, minimap, action pad, terrain, and units rendered on screen; see `macos_port_screenshot_2026-04-20.png`.
- `completed` Native arm64 now reaches a real on-screen world render again after fixing the terrain/generic shadow-sampler conflict; the current screenshot shows playable scene content instead of the earlier blank background.
- `completed` Fresh Apple Silicon stack samples no longer show the previous minute-long startup stall in `R_GL_ImageQuilt_MakeTileset`; after the terrain fallback, the render thread spends its time in normal frame/render work.
- `completed` A fresh native Apple Silicon interaction smoke test now changes visible game state: the camera/view changed, a unit selection ring became visible, and the app stayed responsive during the test.
- `completed` A focused Apple Silicon freeze investigation found and fixed a real scheduler bug in `Sched_Tick()`, where the main thread was waiting on `s_ready_cond` without holding `s_ready_lock`.
- `completed` Minimap interaction on native Apple Silicon is now validated beyond the earlier camera-change smoke test: a stronger lower-left terrain click no longer sends the camera into the off-map black view, and the corrected scene remains stable after a short idle hold.
- `completed` The repeated `Unmatched perf marker: Task 00x [navigation_tick_task]` warning no longer reproduces in a verified 65-second native Apple Silicon idle run after suppressing frame-boundary false positives for worker-thread task stacks.
- `completed` Normal `GL_ASSERT_OK()` behavior is restored on Apple Silicon; the current native launch and idle-smoke path now survives with real OpenGL assertions enabled.
- `completed` A fresh `./bin/pf-arm64 ./ ./scripts/rts/main.py` launch with the restored full Apple Silicon water path again reports only the expected compute-shader skip on this Mac.
- `completed` Full water reflection/refraction rendering is now back on native Apple Silicon: the restored path launches cleanly, stays quiet on stderr after first render, and the latest verified frame shows rippled/refractive water detail instead of the earlier flat fallback.
- `completed` Real shadow-map rendering is now back on native Apple Silicon: after re-enabling the shadow-map bind path, the shadowed terrain shader, and the terrain shadow buffers, a fresh launch stayed quiet on stderr and a live frontmost screenshot showed projected world shadows again.
- `completed` Batch rendering is now back on native Apple Silicon: after re-enabling batched shader init and turning `pf.video.use_batch_rendering` back on, the native arm64 build launched cleanly, stayed quiet on stderr, and the live frame continued to show correct world/entity rendering.
- `completed` `python3 -m py_compile scripts/common/disappearing_text_task.py` now succeeds with the Python 3 compatibility shim for the old `pf.Task`-backed disappearing-text helper.
- `completed` A fresh `make pf PLAT=MACOS_ARM64 MACOS_ARM64_BUILD_READY=1` and `./bin/pf-arm64 ./ ./scripts/rts/main.py` relaunch after the disappearing-text compatibility patch still build and launch cleanly, with no new startup output beyond the expected compute-shader skip.
- `completed` `python3 -m py_compile scripts/common/views/session_window.py scripts/rts/view_controllers/demo_vc.py` succeeds after the native Python 3 session-window update.
- `completed` A fresh `make pf PLAT=MACOS_ARM64 MACOS_ARM64_BUILD_READY=1` after the session-path update still links successfully.
- `completed` A direct runtime probe through `./bin/pf-arm64 ./ /tmp/pf_session_api_check.py` now prints:
  - `PF_SAVE_SESSION_NOT_IMPLEMENTED: Session save is unavailable on Python 3 in the current macOS native port.`
  - `PF_LOAD_SESSION_NOT_IMPLEMENTED: Session load is unavailable on Python 3 in the current macOS native port.`
- `completed` `python3 -m py_compile scripts/rts/views/demo_window.py scripts/rts/view_controllers/demo_vc.py` succeeds after removing the native Python 3 Session entry point from the RTS demo.
- `completed` A fresh `make pf PLAT=MACOS_ARM64 MACOS_ARM64_BUILD_READY=1` and `./bin/pf-arm64 ./ ./scripts/rts/main.py` relaunch after the Session-entry cleanup still build and launch cleanly, with no new startup output beyond the expected compute-shader skip.
- `completed` `python3 -m py_compile scripts/editor/scene.py scripts/rts/units/__init__.py scripts/rts/units/anim_moveable.py scripts/rts/units/anim_combatable.py` succeeds after making the scene writer reusable from Python 3 and making RTS unit-class registration/import safe outside `rts/main.py`.
- `completed` `./bin/pf-arm64 ./ /tmp/pf_native_scene_export.py` now exports a live RTS scene snapshot on Apple Silicon and reports `EXPORTED_SCENE_OBJS=388`.
- `completed` `./bin/pf-arm64 ./ /tmp/pf_native_scene_import.py` now loads that exported snapshot on a fresh Apple Silicon run and reports `IMPORTED_SCENE_OBJS=388`.
- `completed` The native Python 3 RTS-demo session roundtrip now verifies end to end on Apple Silicon:
  - a fresh `./bin/pf-arm64 ./ /tmp/pf_native_session_save.py` writes `tmp_native_session_roundtrip.pfsave`
  - a fresh `./bin/pf-arm64 ./ /tmp/pf_native_session_load.py` now restores that save and writes `NATIVE_SESSION_RESTORED objs=388 regions=0 cameras=0`
- `completed` The re-enabled Python 3 Session UI and normal demo event flow now also verify on Apple Silicon:
  - `python3 -m py_compile scripts/rts/views/demo_window.py scripts/rts/view_controllers/demo_vc.py scripts/common/views/session_window.py /tmp/pf_native_session_ui_roundtrip.py` succeeds
  - a fresh `./bin/pf-arm64 ./ /tmp/pf_native_session_ui_roundtrip.py` now drives the normal demo/UI Session show-save-load flow and writes `NATIVE_SESSION_RESTORED objs=388 regions=0 cameras=0`
- `completed` Native Python 3 save/load now also verifies non-empty scene-region and scene-camera coverage on Apple Silicon:
  - `python3 -m py_compile scripts/editor/scene.py scripts/rts/main.py /tmp/pf_native_session_region_camera_roundtrip.py` succeeds
  - a fresh `./bin/pf-arm64 ./ /tmp/pf_native_session_region_camera_roundtrip.py` now restores `NATIVE_SESSION_RESTORED objs=388 regions=1 cameras=1 region_names=native_session_probe_region camera_names=native_session_probe_camera`
- `completed` The normal Python 3 Session UI flow now also verifies non-empty region/camera coverage on Apple Silicon:
  - `python3 -m py_compile /tmp/pf_native_session_ui_region_camera_roundtrip.py scripts/rts/main.py` succeeds
  - a fresh `./bin/pf-arm64 ./ /tmp/pf_native_session_ui_region_camera_roundtrip.py` now drives the normal demo/UI Session show-save-load flow and restores `NATIVE_SESSION_RESTORED objs=388 regions=1 cameras=1 region_names=native_ui_session_probe_region camera_names=native_ui_session_probe_camera`
- `completed` The previous native Session UI load crash on Apple Silicon is fixed:
  - after repairing task-context render flushing and the macOS movement cleanup path, a fresh `./bin/pf-arm64 ./ /tmp/pf_native_session_ui_region_camera_roundtrip.py` cleanly reaches `NATIVE_SESSION_RESTORED objs=388 regions=1 cameras=1 region_names=native_ui_session_probe_region camera_names=native_ui_session_probe_camera`
  - the final clean stderr for that verification contains only the expected compute-shader skip and no save/load failure output
- `completed` A live native gameplay smoke pass now verifies:
  - keyboard camera movement changes the visible in-game view
  - the selected blue formation shows visible selection rings and activates the action pad
  - a move order executes successfully and visibly repositions the selected formation
  - the move order reveals additional fog-of-war terrain during movement
  - a 65-second hold after the interaction pass produced no new stderr output
- `completed` The current native Apple Silicon RTS smoke-coverage milestone is considered done for this tracker.
- `completed` `python3 -m py_compile scripts/rts/main.py scripts/macos/pf_native_launch_probe.py scripts/macos/pf_native_session_ui_region_camera_roundtrip.py scripts/common/views/session_window.py scripts/rts/view_controllers/demo_vc.py` succeeds after the Apple Silicon polish baseline updates.
- `completed` `make pf PLAT=MACOS_ARM64 MACOS_ARM64_BUILD_READY=1` still links successfully after the Mac-defaults/config cleanup.
- `completed` `make -n run PLAT=MACOS_ARM64 MACOS_ARM64_BUILD_READY=1` now expands to the real native game launch command:
  - `./bin/pf-arm64 ./ ./scripts/rts/main.py`
- `completed` The new repo-backed launch probe now verifies the current Apple Silicon runtime state and exits on its own:
  - a fresh `./bin/pf-arm64 ./ ./scripts/macos/pf_native_launch_probe.py` reports `NATIVE_LAUNCH_READY ... pf.video.vsync=True ... pf.video.use_batch_rendering=True ... pf.game.combat_hz=1.000000`
- `completed` Three consecutive native Apple Silicon relaunch probes now complete cleanly with the same expected settings marker and only the expected compute-shader skip on stderr.
- `completed` The new repo-backed Session UI roundtrip probe now verifies the richer native save/load path from the repo instead of `/tmp`:
  - a fresh `./bin/pf-arm64 ./ ./scripts/macos/pf_native_session_ui_region_camera_roundtrip.py` reports `NATIVE_SESSION_RESTORED objs=388 regions=1 cameras=1 region_names=native_ui_session_probe_region camera_names=native_ui_session_probe_camera`
- `completed` The Apple Silicon settings path now verifies relaunch persistence through repo-backed probes:
  - a fresh `./bin/pf-arm64 ./ ./scripts/macos/pf_native_settings_apply_probe.py` reports `NATIVE_SETTINGS_APPLIED {'pf.video.vsync': False, 'pf.game.healthbar_mode': 2}` and writes the changed values into `pf.conf`
  - a follow-up `./bin/pf-arm64 ./ ./scripts/macos/pf_native_settings_verify_restore_probe.py` reports `NATIVE_SETTINGS_RESTORED {'pf.video.vsync': True, 'pf.game.healthbar_mode': 1}` and restores the baseline config values
- `completed` A real manual Apple Silicon gameplay/settings validation pass now also succeeded in the live game UI:
  - camera movement and minimap navigation stayed on-map
  - unit selection, move, attack, pause/resume, fog-of-war reveal, and Session save/load all passed
  - live settings checks passed in the running game
  - the user reported the end-to-end pass as `pass`
- `completed` The first Metal backend split now builds and verifies on Apple Silicon:
  - `make pf PLAT=MACOS_ARM64 MACOS_ARM64_BUILD_READY=1 RENDER_BACKEND=OPENGL` still builds and the repo-backed launch probe reports `pf.render.backend=OPENGL`
  - `make pf PLAT=MACOS_ARM64 MACOS_ARM64_BUILD_READY=1 RENDER_BACKEND=METAL` now builds the new backend split and the Metal launch probe reports `pf.render.backend=METAL`
  - backend-specific `obj/` trees plus relink-on-run now keep the fallback and Metal binaries from reusing the wrong compiled mode
- `completed` The next Metal UI slice now also verifies on Apple Silicon:
  - `src/render/backend_metal.m` executes real frame begin/end/present work instead of only dropping everything to a clear/present stub
  - `src/render/gl_ui.c` now routes Nuklear font-atlas upload and draw-list rendering through the Metal backend
  - a live Metal run now shows the stock startup menu/UI on-screen against the current blank world background; see `backups/2026-04-22-metal-fork-slice-3/pf_metal_ui_live.png`
  - the stable OpenGL fallback was rechecked immediately afterward and still reports `pf.render.backend=OPENGL`
- `completed` The first Metal terrain slice now also verifies on Apple Silicon:
  - `src/render/render_asset_load.c` now preserves a CPU copy of terrain chunk vertices for Metal builds so the backend can draw real terrain geometry without depending on OpenGL VBO ownership
  - `src/render/backend_metal.m` now consumes real `R_GL_SetViewMatAndPos`, `R_GL_SetProj`, and terrain `R_GL_Draw` commands instead of dropping the entire world path
  - a live Metal run now shows simplified but real terrain geometry behind the working startup UI; see `backups/2026-04-22-metal-fork-slice-4/pf_metal_terrain_live.png`
  - the stable OpenGL fallback was rebuilt and rechecked immediately afterward with `./bin/pf-arm64 ./ ./scripts/macos/pf_native_launch_probe.py`, which again reported `pf.render.backend=OPENGL`
- `completed` The first Metal static-mesh slice now also verifies on Apple Silicon:
  - `src/render/render_asset_load.c` now preserves a CPU copy of non-animated mesh vertices for Metal builds so the backend can draw real static-mesh geometry without depending on OpenGL VBO ownership
  - `src/render/backend_metal.m` now shades non-animated meshes from CPU-side material diffuse colors and consumes their per-entity `R_GL_Draw` calls on Metal
  - `src/game/game.c` now temporarily forces `pf.video.use_batch_rendering` off on the Metal backend so the existing per-entity draw path can advance before Metal batch parity is implemented
  - live verification showed real static-mesh draw calls reaching the Metal backend, and the clean post-check screenshot for this slice is saved at `backups/2026-04-22-metal-fork-slice-5/pf_metal_static_mesh_live.png`
  - the stable OpenGL fallback was rebuilt and rechecked immediately afterward with `./bin/pf-arm64 ./ ./scripts/macos/pf_native_launch_probe.py`, which again reported `pf.render.backend=OPENGL`
- `completed` The first Metal skinned-mesh slice now also verifies on Apple Silicon:
  - `src/render/render_asset_load.c` now marks animated assets as pose-buffer users during asset load and preserves CPU copies of `anim_vert` streams for Metal builds
  - `src/render/backend_metal.m` now consumes `R_GL_AnimSetUniforms` plus animated `R_GL_Draw` calls, computes current joint matrices from the live animation state, and software-skins the first visible units through the Metal mesh pipeline
  - the clean post-check screenshot for this slice is saved at `backups/2026-04-22-metal-fork-slice-6/pf_metal_skinned_mesh_live.png`
  - the stable OpenGL fallback was rebuilt and rechecked immediately afterward with `./bin/pf-arm64 ./ ./scripts/macos/pf_native_launch_probe.py`, which again reported `pf.render.backend=OPENGL`
- `completed` The first Metal world-selection-overlay slice now also verifies on Apple Silicon:
  - `src/render/backend_metal.m` now routes `R_GL_DrawSelectionCircle` and `R_GL_DrawSelectionRectangle` through Metal instead of dropping them
  - those helpers render through the existing Metal world-colored strip path and use the same CPU-side selection radius, OBB, color, and terrain-height data the OpenGL path already uses
  - the clean live screenshot for this slice is saved at `backups/2026-04-22-metal-fork-slice-7/pf_metal_selection_overlay_probe_live.png`
  - the stable OpenGL fallback was rebuilt and rechecked immediately afterward with `./bin/pf-arm64 ./ ./scripts/macos/pf_native_launch_probe.py`, which again reported `pf.render.backend=OPENGL`
- `completed` The first Metal drag-box UI slice now also verifies on Apple Silicon:
  - `src/render/backend_metal.m` now routes `R_GL_DrawBox2D` through Metal instead of dropping the screen-space selection-box command
  - live verification showed the green drag box rendering on top of the Metal scene, and the clean post-check screenshot for this slice is saved at `backups/2026-04-22-metal-fork-slice-8/pf_metal_dragbox_live.png`
- `completed` The first Metal map-overlay-quads slice now also verifies on Apple Silicon:
  - `src/render/backend_metal.m` now routes `R_GL_DrawMapOverlayQuads` through a dedicated blended world-color Metal pipeline instead of dropping the terrain-conforming overlay command
  - the first live verification used `pf.debug.show_faction_vision=1`, and the resulting translucent filled tile-overlay plus outline screenshot is saved at `backups/2026-04-22-metal-fork-slice-14/pf_metal_map_overlay_probe.png`
  - the Metal helper renders the box as a screen-space outline overlay using the same top-left drawable coordinates and signed-size semantics as the OpenGL path
  - the clean live screenshot for this slice is saved at `backups/2026-04-22-metal-fork-slice-8/pf_metal_dragbox_live.png`
  - the stable OpenGL fallback was rebuilt and rechecked immediately afterward with `./bin/pf-arm64 ./ ./scripts/macos/pf_native_launch_probe.py`, which again reported `pf.render.backend=OPENGL`
- `completed` The first Metal gameplay smoke probe now also verifies on Apple Silicon:
  - `scripts/macos/pf_metal_gameplay_smoke_probe.py` now proves the current Metal runtime can move the camera, apply unit selection, accept a move order, pause/resume, and complete an attack/contact step against a staged enemy target without renderer failure
  - the current pass marker is `METAL_GAMEPLAY_SMOKE_PASS backend=METAL camera=1 selection=1 move=1 pause=1 attack=1`
  - this keeps the smoke test narrow and repeatable: it validates the first real gameplay flow on Metal without yet claiming full Session/load/editor parity
- `completed` The first Metal Session UI roundtrip now also verifies on Apple Silicon:
  - the existing repo-backed `scripts/macos/pf_native_session_ui_region_camera_roundtrip.py` probe now passes unchanged on a `RENDER_BACKEND=METAL` build
  - a fresh `./bin/pf-arm64 ./ ./scripts/macos/pf_native_session_ui_region_camera_roundtrip.py` restored `NATIVE_SESSION_RESTORED objs=388 regions=1 cameras=1 region_names=native_ui_session_probe_region camera_names=native_ui_session_probe_camera`
  - the error marker file stayed empty, so the current Metal runtime now proves the normal Python 3 Session UI save/load flow for the richer region/camera case
- `completed` The first live Metal fog-of-war terrain slice now also verifies on Apple Silicon:
  - `src/render/backend_metal.m` now keeps a live fog-state buffer from `R_GL_MapUpdateFog`, binds the terrain uniforms on both the vertex and fragment stages, and shades terrain chunks against the same chunk-major fog state that the OpenGL path receives
  - the clean live screenshot for this slice is saved at `backups/2026-04-22-metal-fork-slice-17/pf_metal_fog_live.png`
  - this restores the core runtime black-shroud / explored-terrain behavior on the Metal path while leaving water and later material/texturing fidelity as separate follow-up slices
- `completed` The first live Metal water slice now also verifies on Apple Silicon:
  - `src/render/backend_metal.m` now builds a real per-tile water mask from the loaded map during `R_GL_MapBegin` and uses that mask inside the Metal terrain fragment path, so visible ponds and lakes render again on the runtime scene instead of the Metal path showing only dry terrain
  - this first slice is intentionally narrow and honest: it restores visible water areas through terrain-integrated wave-tinted shading, but it does not yet claim full reflection/refraction parity with the OpenGL `R_GL_DrawWater` path
  - the clean live screenshot for this slice is saved at `backups/2026-04-22-metal-fork-slice-18/pf_metal_water_live.png`
- `completed` The first real Metal terrain-texturing slice now also verifies on Apple Silicon:
  - `src/render/backend_metal.m` now catches `R_GL_MapInit`, loads the source map textures from `assets/map_textures` into a Metal texture array, and samples that array in the Metal terrain shader through the existing terrain UV/material-index path
  - this lifts the runtime ground from the temporary flat material-index palette into a first real grass/dirt/cobble terrain pass without claiming full OpenGL Wang-tileset parity yet
  - the clean live screenshot for this slice is saved at `backups/2026-04-22-metal-fork-slice-19/pf_metal_textured_terrain_live.png`
- `completed` The first real Metal textured static/skinned-mesh slice now also verifies on Apple Silicon:
  - `src/render/render_private.h` and `src/render/render_asset_load.c` now preserve each mesh asset's `base_path` for Metal so the backend can resolve material textures directly from the original asset folder
  - `src/render/backend_metal.m` now lazily builds a per-mesh material texture array from `material.texname`, binds it for both static and software-skinned animated mesh draws, and samples it through the existing mesh UV/material-index path instead of only using diffuse-color fallback shading
  - the clean live screenshot for this slice is saved at `backups/2026-04-22-metal-fork-slice-20/pf_metal_textured_meshes_live.png`
- `completed` The first Metal alpha-tested foliage/tree slice is now in place on Apple Silicon:
  - `src/render/backend_metal.m` now mirrors the OpenGL cutout-material behavior more closely for alpha-bearing mesh textures: after sampling the mesh material texture array it premultiplies the sampled RGB by alpha before applying the existing `<= 0.5` fragment discard, which stabilizes textured foliage edges instead of treating those pixels like plain opaque solids
  - the Metal mesh material upload path now also inspects loaded material textures for real alpha content and marks those meshes as cutout meshes, so known vegetation assets like `assets/models/pine_tree/pino.png`, `assets/models/shrub/planta.png`, `assets/models/fern/fern_leaf.png`, and the bush textures under `assets/models/bushes/` route through the cutout-safe Metal path automatically
  - for those alpha-bearing cutout meshes, Metal now disables backface culling during the mesh draw so foliage planes do not collapse into missing or dark silhouettes when viewed from the opposite side
- `completed` The first Metal batch-rendering compatibility slice is now in place on Apple Silicon:
  - `src/game/game.c` no longer forces `pf.video.use_batch_rendering` back off when the active backend is `METAL`
  - `src/render/backend_metal.m` now accepts `R_GL_Batch_Draw` and `R_GL_Batch_DrawWithID` by replaying the already-prepared visible animated and static entity lists through the current Metal skinned/static mesh helpers instead of dropping batched scene draws entirely
  - this first slice is intentionally about functional parity, not performance parity: `R_GL_Batch_AllocChunks` and `R_GL_Batch_Reset` are harmless no-ops on Metal for now, `R_GL_Batch_RenderDepthMap` remains deferred with Metal shadow work, and the current value is that batched runtime rendering now stays playable on the Metal path while the stable OpenGL fallback remains untouched
  - the Metal launch probe now reports `pf.video.use_batch_rendering=True`, and `scripts/macos/pf_metal_gameplay_smoke_probe.py` still finishes with `METAL_GAMEPLAY_SMOKE_PASS backend=METAL camera=1 selection=1 move=1 pause=1 attack=1`
- `completed` The first true Metal batch-performance slice is now in place on Apple Silicon:
  - `src/render/backend_metal.m` now goes beyond compatibility replay for opaque static meshes: when Metal receives `R_GL_Batch_Draw` / `R_GL_Batch_DrawWithID`, it groups non-translucent static entities by shared `render_private`, CPU-transforms their vertices into world space, and submits those larger merged vertex streams through the existing Metal static-mesh path with an identity model matrix
  - this is intentionally still a narrow performance slice: animated meshes and translucent static meshes stay on the existing safe per-entity replay path, so we reduce draw-call/setup churn for the biggest easy static case first without risking ordering regressions
  - Metal still verifies with batching enabled after the change: `pf_metal_launch_probe.py` reports `pf.video.use_batch_rendering=True`, and `pf_metal_gameplay_smoke_probe.py` still finishes with `METAL_GAMEPLAY_SMOKE_PASS backend=METAL camera=1 selection=1 move=1 pause=1 attack=1`
- `completed` The next Metal batch-performance slice now extends that path to opaque animated meshes:
  - `src/render/backend_metal.m` now groups non-translucent animated entities by shared `render_private`, CPU-skins each entity into world-space vertices from its live joint pose, and submits those merged animated streams through the existing Metal static-mesh draw path with an identity model matrix
  - this keeps the slice narrow and safe: translucent static meshes and translucent animated meshes still stay on the existing per-entity replay path because blended ordering is still the remaining risky part of Metal batch parity
  - Metal still verifies with batching enabled after the animated extension: `pf_metal_launch_probe.py` reports `pf.video.use_batch_rendering=True`, and `pf_metal_gameplay_smoke_probe.py` still finishes with `METAL_GAMEPLAY_SMOKE_PASS backend=METAL camera=1 selection=1 move=1 pause=1 attack=1`
- `completed` The next Metal batch-performance slice now restores conservative translucent mesh parity:
  - `src/render/backend_metal.m` now uses a dedicated translucent static-mesh pipeline for blended entity draws instead of routing translucent meshes through the opaque pipeline
  - the Metal batch path now handles translucent static and animated meshes conservatively by only merging consecutive entities with the same `render_private`, which preserves draw order while still reducing redundant setup for same-mesh translucent runs
  - the merged-stream helper now draws the full combined vertex count instead of only the source mesh's single-entity vertex count, and the translucent animated fallback now redraws the full failed run instead of silently dropping later entities after a partial merge failure
  - Metal still verifies with batching enabled after the translucent extension: `pf_metal_launch_probe.py` reports `pf.video.use_batch_rendering=True`, and `pf_metal_gameplay_smoke_probe.py` still finishes with `METAL_GAMEPLAY_SMOKE_PASS backend=METAL camera=1 selection=1 move=1 pause=1 attack=1`
- `completed` The first Metal MSAA slice is now in place on Apple Silicon:
  - `src/render/backend_metal.m` now creates a multisampled main-frame color target for Metal and resolves that target into the CAMetalLayer drawable during present instead of rendering the main scene directly into the single-sample drawable
  - the Metal backend now chooses multisampled frame pipelines only for the on-screen frame pass while leaving offscreen passes like the minimap bake/update on their existing single-sample pipelines, so the first anti-aliasing pass improves the real game frame without breaking the already-working auxiliary render paths
  - Metal still verifies cleanly after the MSAA integration: `pf_metal_launch_probe.py` reports `pf.render.backend=METAL`, `pf_metal_gameplay_smoke_probe.py` still finishes with `METAL_GAMEPLAY_SMOKE_PASS backend=METAL camera=1 selection=1 move=1 pause=1 attack=1`, and `pf_native_session_ui_region_camera_roundtrip.py` still restores `objs=388 regions=1 cameras=1`
- `completed` The first Metal minimap slice now also verifies on Apple Silicon:
  - `src/render/backend_metal.m` now routes `R_GL_MinimapBake`, `R_GL_MinimapRender`, and `R_GL_MinimapFree` through Metal instead of dropping the minimap entirely
  - the current slice is intentionally narrow: it bakes a top-down terrain texture, renders the rotated minimap diamond on-screen, routes `R_GL_MinimapRenderUnits` through Metal using the same normalized minimap-space dot sizing and transform as the OpenGL path, and now also draws the minimap camera-frustum overlay through the same CPU-side frustum math with explicit clipping in minimap-local `[-1, 1]` space before screen transform
  - the clean live screenshot for this slice is saved at `backups/2026-04-22-metal-fork-slice-11/pf_metal_minimap_frustum_live.png`
  - the earlier off-minimap frustum leak is gone after the clipped Metal frustum path; fog/water integration and broader gameplay validation remain intentionally deferred for later Metal slices
  - the stable OpenGL fallback was rebuilt and rechecked immediately afterward with `./bin/pf-arm64 ./ ./scripts/macos/pf_native_launch_probe.py`, which again reported `pf.render.backend=OPENGL`
- `completed` The first Metal healthbar slice now also verifies on Apple Silicon:
  - `src/render/backend_metal.m` now routes `R_GL_DrawHealthbars` through Metal instead of dropping the gameplay healthbar overlay
  - the first Metal implementation intentionally keeps the draw simple and robust: it uses the same camera/world-to-screen placement math and half-width/half-height sizing rules as the OpenGL path, then renders a bordered fill bar in screen space
  - the clean live screenshot for this slice is saved at `backups/2026-04-22-metal-fork-slice-13/pf_metal_healthbar_live.png`
  - both the Metal build and the stable OpenGL fallback were rebuilt and rechecked for this slice
- `partial` The Metal backend now verifies cold launch, visible startup UI/menu, visible terrain, a first real terrain-texturing pass, a first real textured static/skinned-mesh pass, a first alpha-tested foliage/tree cutout pass, a first batch-rendering compatibility pass, first true batch-performance passes for opaque static, opaque animated, and conservative translucent meshes, first world-selection overlays, drag-box UI selection, first healthbars, a first minimap render with terrain, unit dots, and clipped camera-frustum overlay, translucent map-overlay quads, a first scripted gameplay smoke pass, the richer Python 3 Session UI save/load roundtrip, live terrain fog-of-war shading, a first visible water slice, and a first on-screen MSAA pass; broader normal play, full reflection/refraction water parity, richer terrain-material parity, frame-pacing/synchronization work, and editor support are still pending migration work.
- `completed` The first Metal smoothness cleanup is now in place:
  - `src/render/backend_metal.m` now caches persistent Metal vertex buffers for terrain chunks and non-animated meshes instead of rebuilding those GPU buffers every frame from CPU-side copies
  - this does not change visual output yet, but it removes one of the current backend's biggest avoidable per-frame costs on Apple Silicon and matches the current migration goal of improving smoothness before adding heavier quality features
  - both `RENDER_BACKEND=METAL` and the stable `RENDER_BACKEND=OPENGL` fallback were rebuilt and rechecked immediately afterward
- `blocked` Rosetta host dependencies are not available under the expected x86_64 prefix on this Mac.
- `blocked` Installing Rosetta Homebrew under `/usr/local` failed because this account does not have the required macOS admin privileges.
- `blocked` Vendored `GLEW` still fails on macOS even without regeneration, with duplicate typedef and symbol definitions in `deps/GLEW/include/GL/glew.h` and `deps/GLEW/src/glew.c`.
- `blocked` Building x86_64 Python `2.7.18` under `pyenv` hit a modern dependency failure because the current python-build flow expects `openssl@1.1`.
- `completed` Native arm64 dependency validation is now green on this Mac.
- `partial` The Python 3 migration is no longer the immediate native-build blocker; the current native frontier has moved from basic launch into deeper functional validation.

## Verification Checklist

- `completed` Save the plan into the repo and track execution status here.
- `completed` Lock the Apple Silicon shipping baseline in repo defaults/config.
- `completed` Make `make run PLAT=MACOS_ARM64` launch the real native game path.
- `completed` Add repo-backed Apple Silicon validation probes for cold launch and Session UI roundtrip coverage.
- `completed` Verify the repo-backed cold-launch probe on this Mac.
- `completed` Verify the repo-backed Session UI roundtrip probe with non-empty region/camera coverage on this Mac.
- `completed` Verify repeated native relaunches on this Mac after the baseline cleanup.
- `completed` Verify settings apply plus relaunch persistence on this Mac and restore the baseline config afterward.
- `completed` Complete the broader Apple Silicon manual gameplay/settings validation pass.
- `completed` Freeze a Metal workstream backup snapshot before renderer changes.
- `completed` Add explicit `RENDER_BACKEND=OPENGL|METAL` support and backend-aware rebuild invalidation.
- `completed` Split shared renderer control from backend-specific OpenGL bootstrap code.
- `completed` Verify the OpenGL Apple Silicon fallback still builds and launches after the backend split.
- `completed` Verify a Metal cold-launch probe on Apple Silicon and surface the active backend through `pf.get_render_info()`.
- `partial` Port the runtime playable scene to Metal (terrain, meshes, minimap, scene presentation) now that startup UI/font rendering is live.
  - simplified terrain is now visible on Metal
  - non-animated mesh draws now reach Metal through the per-entity path, with Metal temporarily forcing batching off until batch parity is ported
  - skinned meshes now reach Metal through a first CPU-skinned per-entity path
  - world selection circles/rectangles now render on Metal
  - drag-box UI selection now renders on Metal
  - healthbars now render on Metal
  - a first minimap render with terrain, unit dots, and clipped camera-frustum overlay now appears on Metal
  - terrain and non-animated mesh vertex data now stay in persistent Metal buffers instead of being re-uploaded every frame
  - the first repo-backed gameplay smoke probe now passes on Metal for camera, selection, move, pause/resume, and attack/contact
  - the existing repo-backed richer Session UI roundtrip probe now also passes on Metal for save/load with non-empty region/camera metadata
  - fog/water integration and broader normal gameplay flow are still pending
- `pending` Port the editor onto the Metal backend after the runtime scene is stable.
- `pending` Remove the temporary Metal dependence on linked OpenGL renderer symbols once the draw-path migration is far enough along.
- `completed` Implement `make deps PLAT=MACOS_X86_64`.
- `completed` Implement `make deps PLAT=MACOS_ARM64`.
- `partial` Implement `make pf PLAT=MACOS_X86_64`.
  - The target and compiler wiring exist, but the machine is still missing the Rosetta dependency/toolchain pieces needed for a real link.
- `completed` Verify the demo on this Mac or record the exact remaining blockers.
- `completed` Remove macOS GLEW dependency from the renderer and dependency checks.
- `completed` Verify the new macOS GL loader and renderer fallbacks at object-build level.
- `completed` Clear the first native-arm64 `gnu11` warning wave that blocked renderer and game objects.
- `completed` Start the Python 3.13 migration enough to keep the arm64 compile moving.
  - The embedded console and custom pickler are stubbed on Python 3 where planned, and the core bridge now compiles far enough for the wider native build to proceed.
- `completed` Implement the first Darwin arm64 scheduler/context-switch backend and verify it compiles.
- `completed` Link a full native `bin/pf-arm64` build on this Apple Silicon Mac.
- `completed` Launch the native arm64 build and clear the first startup/runtime blockers through an early smoke run.
- `completed` Validate deeper functional behavior beyond early launch stability.
  - Visible native demo window and world rendering are confirmed, and a basic interaction pass now shows camera/view change plus unit selection on Apple Silicon.
  - Minimap interaction validation is now green and the old worker perf warning is gone in the latest 65-second idle verification run.
  - A later smoke pass also verified a successful move order, fog-of-war reveal, and a quiet 65-second post-interaction hold.
  - The remaining RTS smoke-coverage milestone is now marked done for the current bring-up tracker.
- `completed` Complete the broader Apple Silicon manual gameplay/settings validation pass.
  - The automated launch/relaunch, Session UI, and settings persistence probes are green.
  - The live user-driven gameplay/settings pass is also now green.
- `completed` Update this file with the verified implementation status.
- `completed` Build the first native scene-snapshot foundation that avoids `S_PickleObjgraph` and verify export/import in fresh arm64 runs.
